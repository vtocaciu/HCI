using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class uneltedoctor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameObject.Find("patrat-st").SetActive(false);
        GameObject.Find("patrat-seringa").SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
